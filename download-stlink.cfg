# OpenOCD 烧录配置 - ST-Link 版本
# 用法: 在 build 目录下执行
#   openocd -f ../download-stlink.cfg
# 或运行 ./run.sh stlink

source [find interface/stlink.cfg]
source [find target/stm32h7x.cfg]

# 相对路径: 从 openocd 的工作目录(build/)定位固件
program dcmi.elf verify reset exit
